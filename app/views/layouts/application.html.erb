<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "Untitled" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "application", 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <header>
      <h2>Treebay</h2>
      <nav>
        <ul>
          <li><%= link_to "Cart", show_cart_path%></li>
        </ul>
      </nav>
    </header>
    <section class="flash">
      <h3><%= flash[:status] == :error ? "A problem occurred: " : "" %><%= flash[:result_text] %></h3>
      <% if flash[:messages] %>
        <ul>
          <% flash[:messages].each do |name, problems| %>
            <% problems.each do |problem| %>
              <li><%= name %>: <%= problem %></li>
            <% end %>
          <% end %>
        </ul>
      <% end %>
    </section>

    <%= yield %>

  </body>
</html>
